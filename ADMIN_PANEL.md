# Админ-панель и Retention

## Настройка

### 1. Добавьте админов в `.env`

```env
ADMIN_USER_IDS=123456789,987654321
```

Где `123456789` и `987654321` - это Telegram user ID админов.

### 2. Как узнать свой Telegram User ID

1. Напишите боту [@userinfobot](https://t.me/userinfobot)
2. Он покажет ваш User ID
3. Добавьте его в `ADMIN_USER_IDS`

## Использование

### Команда /admin

Отправьте боту команду `/admin` для доступа к админ-панели.

### Разделы админ-панели

1. **📊 Общая статистика**
   - Всего пользователей
   - Новых пользователей за 30 дней
   - Активных пользователей за 30 дней
   - Средние метрики (сообщения, фото, дни активности)

2. **📈 Retention**
   - D1 Retention (вернулись на следующий день)
   - D4 Retention (вернулись через 4 дня)
   - D7 Retention (вернулись через неделю)
   - D30 Retention (вернулись через месяц)
   - Общая статистика

3. **📅 Ежедневная активность**
   - Показывает активность за последние 7 дней
   - Уникальные пользователи
   - Количество сообщений
   - Количество фото
   - Количество созданных диалогов

4. **👥 Пользователи**
   - Детальная статистика по пользователям
   - Средние показатели

## Retention метрики

### Что такое Retention?

Retention (ретеншн) - это метрика, показывающая, сколько пользователей возвращаются к боту после первого использования.

### Типы Retention

- **D1 Retention**: Процент пользователей, которые вернулись на следующий день после первого использования
- **D4 Retention**: Процент пользователей, которые вернулись через 4 дня после первого использования
- **D7 Retention**: Процент пользователей, которые вернулись через неделю
- **D30 Retention**: Процент пользователей, которые вернулись через месяц

### Как рассчитывается

```
Retention = (Вернувшиеся пользователи / Новые пользователи) * 100%
```

## Отслеживаемая активность

Система автоматически отслеживает:

1. **Первое использование** (`/start`)
   - Создается запись в `user_retention`
   - Отслеживается активность за день

2. **Отправка сообщений**
   - Увеличивается счетчик сообщений
   - Обновляется `last_seen`
   - Обновляется ежедневная активность

3. **Генерация фото**
   - Увеличивается счетчик фото
   - Обновляется `last_seen`
   - Обновляется ежедневная активность

4. **Создание диалогов**
   - Отслеживается создание новых диалогов
   - Обновляется ежедневная активность

## База данных

### Таблицы

1. **user_retention**
   - `user_id` - ID пользователя
   - `first_seen` - Первое использование
   - `last_seen` - Последнее использование
   - `total_sessions` - Количество сессий
   - `total_messages` - Всего сообщений
   - `total_photos` - Всего фото
   - `days_active` - Количество уникальных дней активности

2. **user_activity**
   - `user_id` - ID пользователя
   - `activity_date` - Дата активности
   - `messages_count` - Сообщений за день
   - `photos_generated` - Фото за день
   - `dialogs_created` - Диалогов за день

## Миграция базы данных

При первом запуске после добавления retention, таблицы будут созданы автоматически через `Base.metadata.create_all()`.

Если нужно создать таблицы вручную:

```python
from app.db import engine
from app.models import Base

async with engine.begin() as conn:
    await conn.run_sync(Base.metadata.create_all)
```

## Примеры использования

### Просмотр статистики

1. Отправьте `/admin`
2. Выберите нужный раздел
3. Просмотрите статистику

### Анализ retention

1. Откройте раздел "📈 Retention"
2. Посмотрите D1, D7, D30 метрики
3. Анализируйте тренды возврата пользователей

### Мониторинг активности

1. Откройте раздел "📅 Ежедневная активность"
2. Просмотрите активность за последние 7 дней
3. Отслеживайте изменения в использовании бота

